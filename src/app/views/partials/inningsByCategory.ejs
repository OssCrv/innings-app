<a href="/dependencia/<%= fkDependency %>/categoria/<%= category.id_category %>" class="col-lg-6 mb-4 btn">
    <div class="card bg-light shadow">
        <div class="card-header">
            <h1 class="text-center">
                <%= category.category_name %>
            </h1>
        </div>

        <table class="table table-striped table-bordered table-lg" cellspacing="0" width="100%">
            <thead>
                <tr>
                    <th scope="col">Número de turno</th>
                    <th scope="col">Documento</th>
                </tr>
            </thead>
            <tbody>

                <% for(let inning of innings) { %>
                    <tr>
                        <th scope="col"><%= inning.document %></th>
                        <th scope="col"><%= inning.inning %></th>
                    </tr>



                    <% } %>
            </tbody>
        </table>



    </div>
</a>